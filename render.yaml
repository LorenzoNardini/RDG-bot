services:
  - type: worker
    name: rdg-bot
    runtime: python
    runtimeVersion: 3.12
    buildCommand: pip install -r requirements.txt && python data/seed.py
    startCommand: python main.py
    autoDeploy: true
